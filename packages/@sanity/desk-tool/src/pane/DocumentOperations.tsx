import * as React from 'react'
import {useDocumentOperationResult} from '@sanity/react-hooks'

export function DocumentOperationResults(props) {
  // const event = useDocumentOperationResult(props.id, props.type)
  return null
  // return <pre>{JSON.stringify(event)}</pre>
}
